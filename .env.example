# OpenAI — used by gpt-4o-mini to render markdown into HTML
OPENAI_API_KEY=sk-...

# GitHub token with `contents:write` on the target repo(s)
GITHUB_TOKEN=ghp_...

# Webhook server (server.js)
PORT=7331
# Optional shared secret — if set, POST /deploy must send header x-deploy-token
DEPLOY_TOKEN=
